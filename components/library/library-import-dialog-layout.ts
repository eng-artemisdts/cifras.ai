/** Variante visual partilhada pelos modais do fluxo de importação por áudio. */
export type LibraryImportDialogLayout = "default" | "split";

/** Alias legado usado em `LibraryImportAudioUploadView`. */
export type ExistingChordDialogLayout = LibraryImportDialogLayout;
