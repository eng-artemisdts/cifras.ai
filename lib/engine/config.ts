// @ts-nocheck
/** Passos do slider de seek (0–1000) — POC. */
export const SEEK_SLIDER_STEPS = 1000;

/** URL vazia: a app define `audio.src` a partir de `meta.audioUrl`. */
export const DEFAULT_AUDIO_URL = "";
